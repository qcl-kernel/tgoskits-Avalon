# CPU-isolated profile: a pinned vCPU stays runnable across guest WFI/standby.
# Timer and virtual-device state are checked at VM-exit boundaries instead of
# adding a shared wait-queue wakeup to the control path.
features = ["rt-poll-idle"]
log = "Info"
target = "aarch64-unknown-none-softfloat"
vm_configs = []
